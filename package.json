{
  "name": "threadwave-skill",
  "version": "0.7.3",
  "private": true,
  "type": "module",
  "description": "Bilingual Twitter/X automation skill suite powered by ThreadWave",
  "files": [
    ".codex-plugin/",
    "skills/",
    "release-index.json",
    "schemas/",
    "scripts/",
    "suite-manifest.json"
  ],
  "scripts": {
    "validate": "node scripts/validate-suite.mjs",
    "version:skill": "node scripts/set-skill-version.mjs",
    "artifacts": "node scripts/build-release-artifacts.mjs",
    "artifacts:index": "node scripts/build-release-artifacts.mjs --write-index",
    "lint": "node --check scripts/build-release-artifacts.mjs && node --check scripts/set-skill-version.mjs && node --check scripts/validate-suite.mjs && npm run validate",
    "release:static": "npm run lint && npm run artifacts && cmp release-index.json dist/release-index.candidate.json",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test",
    "package": "mkdir -p dist && npm pack --pack-destination dist"
  },
  "engines": {
    "node": ">=22"
  }
}
