# Objective

Make every local skill preflight check the four-skill suite, ThreadWave CLI, and Chrome extension, then route any missing module to the machine-readable harness-agent setup guide.

# Final Changes

- Declared `https://www.threadwave.xyz/cli/setup/agent.md` as the single recovery route for missing skill-suite, CLI, or extension modules.
- Changed a first missing-skill result from automatic failure reporting to a normal setup handoff while retaining reports for persistent or inconsistent failures.
- Updated all four skills, bilingual UI phrases, plugin metadata, suite validation, tests, and evals to use the agent guide.
- Preserved original daily-run, post, and reply intent across setup and continued to prohibit the retired `twitter-harness` fallback.

# Final Result

Goal achieved. Every installed skill now checks the same three harness modules and sends the user to one machine-readable setup authority when any module is absent.
